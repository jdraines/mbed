Winnie the Pooh
===

Index this book with the following command (add `uv run` to the beginning of the command if developing
in a virtual env)

```
mbed init -d ./book 
```

Following indexing, you can run the following to search:

```
mbed search "Who found Eeyore's tail?" -d ./book
```

